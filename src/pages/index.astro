---
// Welcome to Astro! Everything between these triple-dash code fences

import { fetchCodes } from "@/utils/fetchCodes";

// is your "component front matter". It never runs in the browser.
console.log("This runs in your terminal, not the browser!");

const toolCodes = await fetchCodes();
---

<!-- Below is your "component template." It's just HTML, but with
     some magic sprinkled in to help you build great templates. -->
<html>
  <head>
    <meta charset="utf-8" />
  </head>

  <body>
    <div>
      {
        Object.keys(toolCodes).map((toolCode) => (
          <a href={`tool-codes/${toolCode}`}>{toolCode}</a>
        ))
      }
    </div>
  </body>
</html>
<style>
  h1 {
    color: orange;
  }
</style>
